# 2026-03-08

- User clarification: final deliverable is an inference framework that can run DeepSeek-V2-Lite.
- Near-term: build out the framework skeleton first (model-dir discovery, config parsing, safetensors header parsing/verification, CLI subcommands), then iterate toward real inference.
